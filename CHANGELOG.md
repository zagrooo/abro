# آبرو — تاریخچه‌ی نسخه‌ها

نسخه‌بندی طبق [SemVer](https://semver.org/lang/fa/): `MAJOR.MINOR.PATCH`

| بخش | چه وقت بالا می‌رود |
|---|---|
| **MAJOR** | تغییری که ذخیره‌ی قبلی را می‌شکند یا بازی را از پایه عوض می‌کند |
| **MINOR** | محتوا یا قابلیت جدید، سازگار با ذخیره‌ی قبلی |
| **PATCH** | فقط رفع ایراد و تنظیم ریز |

هر نسخه یک کپی کامل در `archive/<نسخه>/` دارد، همراه با `CHANGELOG.md` خودش.
نسخه‌ی جاری همیشه در ریشه‌ی پروژه است و در تب «دفتر» نمایش داده می‌شود.

---

## نسخه‌ها

| نسخه | تاریخ | خلاصه |
|---|---|---|
| [**v2.3.0**](archive/v2.3.0/CHANGELOG.md) | ۱۴۰۴/۰۵/۲۰ | اصالت دیگر با مواد معمولی هم به صفر نمی‌رسد (هدف به‌جای شیب)، محاسبه‌ی قیمت ۲۶ برابر سریع‌تر، چیدن خودکار نیروها، تور نجات صفحه‌ی بارگذاری |
| [**v2.2.0**](archive/v2.2.0/CHANGELOG.md) | ۱۴۰۴/۰۵/۲۰ | ممیزی ساختاری: حفاظ حلقه‌ی اصلی، سوءاستفاده‌ی تقویت روی شب بسته، سهمیه‌ی تبلیغ که پر نمی‌شد. بازطراحی نوار پرده: فضا و سطح لازم و ضریب اصالت حالا دیده می‌شوند |
| [**v2.1.0**](archive/v2.1.0/CHANGELOG.md) | ۱۴۰۴/۰۵/۲۰ | رفع کرش و هنگ اندروید (طوفان resize، بوم ۱۵ مگابایتی، چگالی پیکسل)، کادوی خیابانی با گزینه‌ی تبلیغ سه‌برابری، تب‌های واکنش‌گرا |
| [**v2.0.0**](archive/v2.0.0/CHANGELOG.md) | ۱۴۰۴/۰۵/۲۰ | **شکستن ذخیره.** اقتصاد از پایه بازنویسی شد (بازی از ۱۵ دقیقه به ده‌ها ساعت رسید)، صفحه‌ی بارگذاری متحرک، فروشگاه الماس، حالت «مغازه بسته»، دو ایستگاه تازه |
| [**v1.1.1**](archive/v1.1.1/CHANGELOG.md) | ۱۴۰۴/۰۵/۲۰ | رفع پریدن نمای فرود؛ ارقام فارسی برای شماره‌ی نسخه؛ کش‌شکن هم‌نام نسخه |
| [**v1.1.0**](archive/v1.1.0/CHANGELOG.md) | ۱۴۰۴/۰۵/۲۰ | بازنویسی خط لوله‌ی رندر: بلوم، درجه‌بندی رنگ، چرخه‌ی شب و سحر، بازتاب آسفالت، عمق میدان، نور لبه‌ای، پرنده و گربه |
| [**v1.0.0**](archive/v1.0.0/CHANGELOG.md) | ۱۴۰۴/۰۵/۲۰ | انتشار پایه: تفکیک به چند فایل، ۱۲ پرده، ۱۰ ایستگاه، ۲۰ رویداد، ۴ شخصیت، دفترچه‌ی پدر، ۱۶ نشان، ۲ پایان |

---

## ساختار پروژه

```
abro/
├── index.html              ← بازی از این‌جا باز می‌شود
├── CHANGELOG.md            ← همین فایل
├── css/
│   ├── fonts.css           وزیرمتن جاسازی‌شده (base64)
│   ├── base.css            توکن‌ها، ریست، پوسته‌ی دستگاه
│   ├── loader.css          صفحه‌ی بارگذاری
│   ├── ui.css              HUD، تب‌ها، شیت، کارت‌ها
│   └── modals.css          رویداد، شیفت، فاکتور، پایان
├── js/
│   ├── util.js             ابزار پایه و شماره‌ی نسخه
│   ├── audio.js            WebAudio: جلوه‌ها و موسیقی
│   ├── data.js             پرده‌ها، ایستگاه‌ها، رویدادها، آدم‌ها، ثابت‌های اقتصاد
│   ├── state.js            وضعیت، ذخیره، فرمول‌ها
│   ├── scene.js            موتور تصویر
│   ├── loader.js           اجرای مرحله‌ای بارگذاری
│   ├── ui.js               رابط کاربری
│   └── game.js             حلقه‌ی اصلی و منطق
└── archive/
    ├── abro-single.html    نسخه‌ی تک‌فایلی قبل از نسخه‌بندی
    ├── v1.0.0/
    ├── v1.1.0/
    ├── v1.1.1/
    ├── v2.0.0/
    ├── v2.1.0/
    ├── v2.2.0/
    └── v2.3.0/
```

## اجرای محلی

با دابل‌کلیک روی `index.html` کار می‌کند. اگر خواستی روی سرور محلی ببینی:

```bash
python -m http.server 8123 --bind 127.0.0.1
```

بعد `http://127.0.0.1:8123/` را باز کن. (`.claude/launch.json` هم برای همین هست.)

## روال انتشار نسخه‌ی بعد

1. تغییرها روی فایل‌های ریشه اعمال شود.
2. `A.VERSION` در `js/util.js` بالا برود.
3. پارامتر `?v=` لینک‌های `index.html` روی **همان شماره‌ی نسخه** تنظیم شود
   (`?v=1.1.2`) — کش‌شکن.
4. کپی کامل در `archive/<نسخه>/`.
5. `CHANGELOG.md` آن نسخه نوشته شود.
6. یک سطر به جدول بالا اضافه شود.
